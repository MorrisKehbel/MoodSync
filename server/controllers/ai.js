import { isValidObjectId } from "mongoose";
import User from "../models/User.js";

export const runAiScore = () => {
  return;
};
export const runAiDecisions = () => {
  return;
};
export const getAiHistory = () => {
  return;
};
